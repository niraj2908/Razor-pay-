# packages/razorpay

The Razorpay Adapter (Section 29). All Razorpay API/webhook calls go through here - application code never calls Razorpay directly. This is what gets mocked in tests (Section 34).
