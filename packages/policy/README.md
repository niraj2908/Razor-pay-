# packages/policy

Shared types/interfaces for MerchantPolicy rules, consumed by services/decision-engine and apps/api. LLM #1 (Merchant Policy Interpreter, Section 15) writes into this shape; deterministic code enforces it.
