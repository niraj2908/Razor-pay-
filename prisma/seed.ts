// Seed script placeholder.
// Will be populated once services/simulator exists -- the simulator is the
// real source of seed data for local development (20K-event batch, or a
// small slice of it for fast local iteration).

async function main() {
  console.log("No seed data yet -- see services/simulator.");
}

main();
