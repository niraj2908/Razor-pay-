# apps/web

Next.js + TypeScript frontend. Consumes apps/api - never calls Razorpay or the database directly (Section 29).

Screens to build (Section 31, in priority order per the execution calendar):
1. Recovery Overview
2. Recovery Queue
3. Decision Detail
4. Experiments
5. Audit
6. Network Incidents / Policies / Assistant / System Health (Next-priority tier)

UI must avoid the generic-AI-dashboard aesthetic (Section 30): no neon/glassmorphism/glowing cards. Target "modern financial operations software" instead.
