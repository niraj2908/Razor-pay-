# services/simulator

Deployable wrapper around the research simulator in /simulator (root). Exposes the 20K-event synthetic batch (with hidden causal ground truth, Section 12) to the rest of the system for baseline/experiment runs.
